//Prompt and Alert lesson.
var width = prompt("We are calculating the area of a rectangle. Please enter the width.");
var height = prompt("Please enter the height.");
var area = width * height; // calculates the specified info.
//console.log(area); //prints out to the console
var result = "The area of the rectangle is " + area + "square ft.";
alert(result);